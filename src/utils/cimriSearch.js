export default {
    async searchCimri(link) {
        const linkSplit = link.split(' ')
        if (linkSplit.length === 1) {
            console.dir(linkSplit);
        }
        else {

        }


    }
}